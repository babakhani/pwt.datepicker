import DefaultConfig from "config";
export function Datepicker(inputElement, options: DefaultConfig) {
    console.log('inputElement and options')
    console.log(inputElement)
    console.log(options)
}